var config = {
    type: Phaser.AUTO,
    width: 2130,
    height: 1200,
    scene: [
        SceneMainMenu,
        // SceneMain,
        // SceneSingleQuestion
    ]
}

var game = new Phaser.Game(config)