var config = {
    type: Phaser.WEBGL,
    width: 2130,
    height: 1200,
    scene: [
        SceneMainMenu,
        // SceneMain,
        // SceneSingleQuestion
    ]
}

var game = new Phaser.Game(config)